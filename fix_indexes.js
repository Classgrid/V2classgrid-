require('mongoose').connect(process.env.MONGO_URI || 'mongodb+srv://classgrid-admin:pass123@classgrid.sa5ww0z.mongodb.net/classgrid?retryWrites=true&w=majority&appName=Classgrid').then(async (m) => { console.log('Connected DB'); const fs = require('fs'); const path = require('path'); const modelsDir = path.join(__dirname, 'src/models'); fs.readdirSync(modelsDir).filter(f => f.endsWith('.js')).forEach(f => { try { require(path.join(modelsDir, f)); } catch(e) {} }); for (const modelName of m.modelNames()) { try { console.log('Dropping ' + modelName); await m.model(modelName).collection.dropIndexes(); } catch(e) { console.log('Err drop: ' + e.message); } } for (const modelName of m.modelNames()) { try { console.log('Creating ' + modelName); await m.model(modelName).createIndexes({ background: false }); } catch(e) { console.error('ERR create ' + modelName + ': ' + e.message); } } console.log('Done'); process.exit(0); });
